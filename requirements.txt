# ChewML — dependencies
# Verified with Python 3.14 on Windows 11 (2026-09-07).

# --- Core: data handling, features, models ---
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0

# --- Plotting (notebooks + report figures) ---
matplotlib==3.10.8
seaborn==0.13.2

# --- Live app (ml_httpstreaming/classifier_app.py) ---
# Terminal UI only; the HTTP server itself uses the standard library.
rich==15.0.0

# --- Running the notebooks ---
ipykernel
nbconvert==7.17.1
nbformat==5.11.1

# --- Only needed for NB10 (1D-CNN on raw signals); everything else runs without it ---
torch==2.11.0
